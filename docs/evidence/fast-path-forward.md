# Fast Path Forward Check

Date: 2026-08-13

## Same scenario

Create a five-slide bilingual strategy deck as editable PPTX, standalone HTML, and PDF within a short deadline. The brief is complete, one generated hero image is allowed, and routine questions should not block execution.

## Expected route-scoped behavior

The updated root Skill now selects Fast Path after request normalization, while retaining runtime resolution, preflight, recommendation, exact-data detection, and routing. The dual-format request shares content and asset manifests, then loads only the active Guizang design guidance, Baoyu image capability, PPT Master native renderer, and Frontend Slides web renderer.

Native PowerPoint rendering and browser interaction remain separate acceptance gates. Exact data, template reconstruction, advanced animation, narration, complex provider work, or material ambiguity explicitly escalates to the complete workflow without discarding valid preflight or routing evidence.

## Evidence

The structural forward contract verifies that the root links `references/fast-path.md` and that the reference contains all escalation and truthful-status terms. Artifact-level evidence is supplied by the bilingual PPTX/HTML/PDF example acceptance suite.
