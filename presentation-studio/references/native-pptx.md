# Native PPTX Authority

Read `engines/ppt-master/SKILL.md`, run its attribution guard unchanged, read its routing workflow, and select exactly one top-level route. Keep the guarded engine files intact.

Use PPT Master for editable shapes, connectors, text, tables, charts, masters, layouts, placeholders, themes, notes, animations, transitions, narration, template fill, and existing-PPTX enhancement.

Preserve master → layout → slide inheritance when editing a template. Prefer native charts/tables and structured shapes. Do not convert a native request into HTML screenshots or full-slide images.

Run the selected workflow's OPC/delivery/edit-safety checks. Render every slide when a renderer is available and inspect full-size output after the last repair.

If PowerPoint, LibreOffice, fonts, narration, or a provider is missing, isolate the affected capability. Do not mark unrendered visual QA or unexecuted desktop behavior as PASS.
